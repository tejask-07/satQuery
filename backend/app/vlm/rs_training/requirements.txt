# Optional RS-VLM training/inference environment. Keep separate from the API runtime.
torch>=2.6,<2.9
transformers>=4.49,<5
peft>=0.14,<0.19
accelerate>=1.3,<2
datasets>=3.2,<5
qwen-vl-utils>=0.0.10,<0.1
PyYAML>=6.0,<7
safetensors>=0.5,<1
# Install bitsandbytes only for a verified CUDA QLoRA environment.
# bitsandbytes>=0.45
